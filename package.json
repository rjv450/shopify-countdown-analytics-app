{
  "name": "countdown-timer-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "app",
    "extensions/countdown-timer-src"
  ],
  "scripts": {
    "dev": "shopify app dev",
    "build": "npm run build:frontend && npm run build:extension",
    "build:frontend": "cd app/frontend && npm run build",
    "build:extension": "cd extensions/countdown-timer-src && npm run build",
    "build:all": "npm run build:frontend && npm run build:extension",
    "deploy": "shopify app deploy",
    "test": "npm run test --workspaces",
    "install:all": "npm install && cd app && npm install && cd ../app/frontend && npm install && cd ../../extensions/countdown-timer-src && npm install"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0",
    "@shopify/theme": "^3.0.0"
  }
}

